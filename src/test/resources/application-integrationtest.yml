spring:
  data:
    mongodb.host: localhost
    mongodb.port: ${MONGODB_PORT}
    mongodb.uri: mongodb://localhost/test-database
    mongo.repositories.enabled: true
  liquibase:
    change-log: classpath:liquibase/changelog_master.xml
    contexts: test